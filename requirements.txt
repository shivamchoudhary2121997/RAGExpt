# ── Streamlit ─────────────────────────────────────────────────────────────────
streamlit>=1.35.0

# ── LangChain stack ───────────────────────────────────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-chroma>=1.0.0        # 1.x required for chromadb 1.x compatibility
langchain-groq>=0.1.6
langchain-huggingface>=0.0.3
langchain-text-splitters>=0.2.0

# ── Vector store ──────────────────────────────────────────────────────────────
chromadb>=1.0.0

# ── OpenTelemetry (chromadb dependency) ───────────────────────────────────────
# opentelemetry-proto 1.11.x ships stale _pb2.py files that crash with
# protobuf >=4.x on Python 3.12+. Pin to >=1.12.0 where protos were regenerated.
opentelemetry-proto>=1.12.0
opentelemetry-exporter-otlp-proto-grpc>=1.12.0

# ── PDF parsing ───────────────────────────────────────────────────────────────
# docling-slim[standard] provides all PDF/table/figure parsing but omits the
# OCR extras (rapidocr, easyocr) which hard-depend on full opencv-python.
# Full opencv-python requires libGL + libgthread system libs absent on
# Streamlit Cloud. Since rag_pipeline.py sets do_ocr=False this is no loss.
docling-slim[standard]>=2.92.0

# ── Embeddings ────────────────────────────────────────────────────────────────
sentence-transformers>=3.0.0
torch>=2.1.0                   # required by sentence-transformers; CPU build is fine
transformers>=4.40.0

# ── Utilities ─────────────────────────────────────────────────────────────────
Pillow>=10.0.0